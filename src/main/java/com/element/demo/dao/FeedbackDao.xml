<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.element.demo.dao">
    <select id="listAllFeedbacks" resultType="com.element.demo.entity.FeedbackEntity">
        select * from feedback
    </select>
    <select id="listFileFeedbacks"  parameterType="String" resultType="com.element.demo.entity.FeedbackEntity">
        select * from feedback where file_name = #{fileName}
    </select>
    <insert id="insertFeedback" parameterType="com.element.demo.entity.FeedbackEntity">
        insert into feedback (serial_num, time, qa_type, source, institution_type, province, city, district, content_type, content, contact, status, file_name) values (#{serialNum}, #{time}, #{qaType}, #{source}, #{institutionType}, #{province}, #{city}, #{district}, #{contentType}, #{content}, #{contact}, #{status}, #{fileName})
    </insert>
</mapper>